function doubleArrayValues(array) {
    for (let i = 0; i < array.length; i++) {
        array[i] *= 2;
    }
    return array;
}

//answer: O(n) The time it takes to complete the function is directly perportinal to the input.